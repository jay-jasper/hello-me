# Design

## Concept

**深渊下潜 (The Descent)**——时间即深度。整站是一次从月光海面到海底的下潜：
滚动 = 下潜 = 回溯人生。记忆是黑暗中发光的生物，光标是探照灯，海底是出生之地。

场景句：深夜潜水员的下潜——头灯之外是无垠墨蓝，一只琥珀色的灯笼在远处安静呼吸。
（暗色不是主题选项，是场景本身；无浅色模式。）

## Color

策略：**Drenched**——表面即水体。tinted 深蓝黑背景的环境理由：surface IS the deep ocean。
种子色 seed-167 `oklch(0.495 0.134 36)` → 主色为深海生物荧光的琥珀（鮟鱇鱼诱饵光），
反手避开"深海=青蓝色"的一阶反射；冷青绿只作次级辉光。

```css
:root {
  /* 水体（按深度渐变，这两端之间插值） */
  --water-shallow: oklch(0.22 0.05 235);  /* 月光层 */
  --water-abyss:   oklch(0.07 0.02 250);  /* 深渊 */
  --bg:            oklch(0.10 0.025 245); /* 基准（HUD/面板底） */
  --surface:       oklch(0.15 0.03 240);  /* 阅读面板 */

  --ink:     oklch(0.94 0.012 220);  /* 正文，vs --bg ≈ 15:1 */
  --muted:   oklch(0.72 0.02 230);   /* 次级文字，vs --bg ≥ 5:1 */

  --lure:    oklch(0.78 0.14 36);    /* 主色：琥珀诱饵光（记忆、CTA、焦点） */
  --plankton:oklch(0.60 0.10 210);   /* 辅色：冷青荧光（次级发光、水母） */
}
```

- 发光 = 注意力等级：琥珀（可交互/重要）> 冷青（氛围/次级）> 无光（背景）。
- 文字永远在 DOM 层，出现时特效退为背景；对比度绝不因"发光感"妥协。

## Typography

品牌声词：**幽深、温柔、发光**。

- **正文/标题（中文）**：霞鹜文楷屏幕阅读版 LXGW WenKai Screen——手写楷意的日记温度，
  对抗冷黑水体；也是"记录一生"的文体本身。单家族多字重承载层级。
- **仪表（拉丁/数字）**：Fragment Mono——深度计、年份、坐标等 HUD 读数。
  mono 在此有字面理由：它就是潜水仪表盘。
- 流式标尺 clamp()，级差 ≥1.25；display 上限 ≤ 5rem。
- 暗底浅字三轴补偿：line-height +0.05~0.1、letter-spacing +0.01em、正文字重上调一档。
- 正文测量 ≤ 42em（中文按字宽），HUD 全大写拉丁加 0.08em 字距。

## Layout / IA

单页长滚，深度即信息架构（自上而下）：

| 深度 | 区域 | 内容 |
|---|---|---|
| 0m | 海面 | 名字 + 一句话 + 月光碎银 + 下潜提示（名片层） |
| -20m | 现在 | 正在做的事、社交链接（发光小鱼群） |
| -50m ~ -900m | 记忆带 | 人生片段 = 发光生物，按年份映射深度，靠近亮起、展开故事 |
| 海底 | 起点 | 出生之地，海床裂隙透出暖琥珀光，结语 |

- HUD 固定：左侧深度计（-XXXm + 对应年份），右下滚动提示。深度→年份的映射把隐喻挑明。
- 记忆内容与特效解耦：`src/content/` 纯数据，站主加内容不碰特效代码。
- 阅读态：展开的故事面板是 DOM 元素（--surface 底 + 模糊水体背景），65ch 内测量。

## Motion

- **签名时刻**：首次滚动的下潜过渡——月光层撕开，海雪开始上升（相对下潜）。
- 滚动驱动相机 Y（lerp 平滑）；越深，动效时长越长（水的阻尼感，600ms → 900ms）。
- 记忆生物：正弦漂移 + 呼吸脉冲；靠近（光标探照灯半径内）时 bloom 亮起。
- 缓动：ease-out-quint / expo，无弹跳。面板展开 400ms，退出 300ms。
- **prefers-reduced-motion**：完整替代——无漂移无平滑滚动，深度分层静态渐变 + 交叉淡入，
  发光保留（静态），体验不打折。

## Time & Light

- **现实时刻决定海面光色**（`MOOD_STOPS`，main.ts）：深夜月光 → 清晨熹微(5-7) → 白昼日光(10-16) → 黄昏余晖(19-21)，按小时插值。水体色由 CSS 变量驱动，天体/光束颜色传入场景。
- **光随深度衰减**：150m 起视野边缘被 `#dim` 径向暗角吞没（最深 0.75）；海雪透明度随深度递减；两屏后天光消失。深处只剩生物荧光。

## Fauna（深度分带物种，scene.ts FISH_SPECS）

浅层是逆光剪影，深层只剩生物荧光——物理正确即美学正确。

深度带刻意重叠，任何深度同屏 3~5 种；群居物种拆多群（pods）散布带内。

| 层 | 物种（17 种） |
|---|---|
| 阳光层 30–360m | 沙丁鱼群×2、金枪鱼群、海龟、蝠鲼、鲸（远景层 z=-6，移动端保留） |
| 暮光层 200–650m | 磷虾群×2、水母×2 群、斧头鱼群×2、灯笼鱼群×2、栉水母 |
| 深渊层 450–950m | 乌贼、桶眼鱼、小飞象章鱼、蝰鱼、深海虾×2 群、吞噬鳗、鮟鱇 |

深渊刻意比暮光层稀疏——空旷是神秘感的一部分。所有鱼为 canvas 白色剪影贴图 + 材质染色的共享 SpriteMaterial；行为四类（school/glide/drift/dart）；reduced-motion 下静止悬浮。

## Effects (WebGL, three.js)

- 水体：全屏深度渐变 shader（--water-shallow → --water-abyss 按相机深度插值 + 低频噪声）。
- 海雪：分层 Points 粒子视差；浅层焦散光斑；表层体积光束（旋转渐变面片，随深度衰减）。
- 发光生物：canvas 生成的辐射纹理 sprite + UnrealBloom（阈值收紧，只让发光体入 bloom）。
- 光标探照灯：跟随点光 + 径向暗角。
- 预算：DPR ≤ 2，移动端粒子减半、关 bloom；WebGL 不可用 → CSS 分层渐变降级，内容完整。

## Components

- **HUD 深度计**：Fragment Mono，tabular-nums，muted 色，当前区域高亮 lure 色。
- **记忆面板**：surface 底、1px 内描边 oklch(0.3 0.03 240)、backdrop-blur 水体、
  标题 WenKai Medium、正文 WenKai Regular、年份 Fragment Mono lure 色。
- **焦点环**：2px lure 色外发光环（box-shadow 双层模拟辉光），键盘可见。
- **链接小鱼**：社交链接 = 发光图标 + 文字，hover 时亮度 +、微上浮。

## Bans（本项目自查）

- 深海≠太空：禁星野粒子背景；深海≠恐怖：禁 jump scare、压迫音效。
- 禁模板 portfolio 语法：技能条、头像卡、identical card grids、每节 eyebrow。
- 禁 gradient text、禁玻璃拟态默认化（backdrop-blur 只用于阅读面板的水体理由）。
- 特效永不遮挡/延迟内容获取：任何文字出现即达标对比度。
